import { IState } from './Interfaces';

const defaultState: IState = {
  vertical: false,
  range: false,
  tip: false,
  bar: false,
  min: 0,
  max: 100,
  step: 1,
  from: 0,
};

export default defaultState;

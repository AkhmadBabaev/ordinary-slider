import OSlider from './Ordinary-slider/OSlider';

window.addEventListener('load', () => {
  // eslint-disable-next-line no-unused-vars
  const oSlider = new OSlider(document.querySelector('.slider-carrier'));
});

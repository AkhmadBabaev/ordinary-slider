mixin checkbox({ additionalClasses, mods, id = generateID(), title, name, checked } = {})
  -
    const className = 'checkbox';
    const classes = generateClassList({ className, mods, additionalClasses });

  div(class= classes)
    +input({
      additionalClasses: `${className}__input`,
      type: 'checkbox',
      id,
      name,
      checked,
    })

    label(class= `${className}__label` for= id)
      span(class= `${className}__title`)= title
      div(class= `${className}__toggler`)
